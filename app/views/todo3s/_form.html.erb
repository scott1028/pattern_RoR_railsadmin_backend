<%= form_for(@todo3) do |f| %>
  <% if @todo3.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@todo3.errors.count, "error") %> prohibited this todo3 from being saved:</h2>

      <ul>
      <% @todo3.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :label %><br>
    <%= f.text_field :label %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
